export type Props = {
  title: string;
  subtitle?: string;
  srcAltered: string;
  srcColor: string;
  storybook?: boolean;
  type: "tech" | "social" | "timeline";
  extraClass?: string;
  smaller?: boolean;
};
